print("Hello " + input("What is your name?\n") + "!")
# In this code, we are using the input() function to get input
# from the user. The text inside the parentheses and quotes is
# a prompt that will be displayed to the user. The input()
# function will wait for the user to enter something and then
# return that input as a string. We are then concatenating
# the string "Hello ", the user's input, and "!" to create
# a personalized greeting.
